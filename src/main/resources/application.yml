server:
  error:
    include-stacktrace: on_param
  port: 3000

logging:
  level:
    org.hibernate:
      SQL: DEBUG
      type: trace

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/movies?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
